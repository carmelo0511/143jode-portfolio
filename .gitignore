# local tooling / preview config
.claude/

# reference material + internal notes (not part of the site)
_research/
MISSION.md

# Sanity Studio — a SEPARATE app (its own deps + deploy via `sanity deploy`);
# the static portfolio that Vercel serves doesn't include it
studio-143jode/

# Vercel
.vercel/

# OS / editor junk
.DS_Store
*.mov
Thumbs.db
